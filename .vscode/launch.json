{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch MCP Camera (stdio)",
      "program": "${workspaceFolder}/dist/index.js",
      "args": ["stdio"],
      "preLaunchTask": "tsc: build - tsconfig.json"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Launch MCP Camera (http)",
      "program": "${workspaceFolder}/dist/index.js",
      "args": ["http"],
      "preLaunchTask": "tsc: build - tsconfig.json"
    }
  ]
}
